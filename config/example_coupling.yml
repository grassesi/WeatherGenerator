rollout:
  start_date: 2023-01-01T00:00
  end_date: 2023-12-31T00:00
  chunk_length: '24:00:00'
  num_chunks: 124
  num_samples: 1
  forecast_offset: 1
  # num_workers: 0        # DataLoader workers per component. 0 loads in the main process.
  # accumulate_chunks: false  # keep every chunk's state to score the assembled rollout

couplings:
  sst_from_ocean:
    producer: Ocean
    consumer: null # => Atmo, once the exchange is wired up
    stream: ERA5-Ocean

  atmo_from_atmo:
    producer: Atmo
    consumer: null # => Ocean, once the exchange is wired up
    stream: ERA5